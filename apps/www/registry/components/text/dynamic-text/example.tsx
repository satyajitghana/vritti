"use client";

import DynamicText from "./component";

export default function DynamicTextExample() {
  return (
    <div className="flex items-center justify-center p-8">
      <DynamicText />
    </div>
  );
}
