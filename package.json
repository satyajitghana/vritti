{
  "name": "vritti-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "pnpm run build:registry && turbo run build",
    "build:registry": "tsx scripts/build-registry.ts",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "turbo": "^2.5.8",
    "tsx": "^4.21.0"
  },
  "packageManager": "pnpm@9.0.0"
}
